import sys

sys.stdin = open("test",'r')
N,K = map(int, input().split())
lst = list(map(int, input().split()))

def check(idx):
    total = 0
    for v in range(lst):
        total 

for idx in range(1, N + 1):
    check(idx)